# Twilio Voice Agent - Dependencies
# Python 3.10+ required

# Core
fastapi==0.128.0
granian==1.6.3      # Replaces uvicorn
uvloop==0.21.0; platform_system != "Windows"      # Faster asyncio (not supported on Windows)
msgspec==0.18.6     # Faster JSON
orjson==3.10.11     # Fastest JSON for FastAPI responses
uvicorn[standard]==0.40.0   # Backup server (includes uvloop)
websockets==15.0.1
python-dotenv==1.2.1
httpx==0.28.1

# Twilio
twilio==9.9.0

# Deepgram STT
deepgram-sdk==5.3.0

# Cartesia TTS
cartesia==2.0.17

# Groq LLM
openai==2.14.0
groq==1.0.0

# Structured extraction
instructor==1.13.0
pydantic==2.12.5

# Semantic routing (enables fast cached responses)
semantic-router[fastembed]==0.1.12
fastembed==0.3.6

# Audio
# numpy is used in tests and optional utility code paths.
numpy

# Contextual TTS microservice dependencies (torch/transformers) are isolated to:
# `csm_service/requirements.txt` so Railway CPU deploys stay lightweight.

# Async
aiofiles==24.1.0

# Logging
structlog==25.5.0

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0

# Twilio Voice Agent - Dependencies
# Python 3.10+ required

# Core
fastapi
websockets
python-dotenv
httpx

# 2025 Performance Stack (Rust-based for low latency voice)
granian>=1.6.0        # Rust HTTP/WebSocket server (replaces uvicorn)
uvloop>=0.21.0        # Faster asyncio event loop (Linux only)
msgspec>=0.18.0       # Fast JSON parsing (replaces json module)

# Twilio
twilio

# Deepgram STT
deepgram-sdk

# Cartesia TTS
cartesia

# Groq LLM
openai
groq

# Structured extraction
instructor
pydantic

# Semantic routing (enables fast cached responses)
semantic-router[fastembed]
fastembed

# Audio
numpy
scipy

# Async
aiofiles

# Logging
structlog

# Testing
pytest
pytest-asyncio

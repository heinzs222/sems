{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "granian --interface asgi server.app:app --port $PORT --host 0.0.0.0",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
